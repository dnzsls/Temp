print(list(df_ekip_model2.columns))
for _c in ('line_based_main_group', 'kuyruk', 'queue'):
    if _c in df_ekip_model2.columns:
        show(df_ekip_model2.groupby(_c)['kisi_sayisi'].sum().reset_index())
