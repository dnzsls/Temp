for ad, d in (('PLAN', df_genel_e), ('MODEL', df_model_e)):
    t = pd.to_datetime(d['data_date'])
    print(f"{ad:<5} {t.min():%d.%m} – {t.max():%d.%m}  |  {t.dt.normalize().nunique()} gün  "
          f"|  toplam çağrı {int(d['cagri'].sum()):,}")
print(f"YEAR/MONTH {YEAR}/{MONTH}  |  MODEL {MODEL_YEAR}/{MODEL_MONTH}  |  SON_GUN {SON_GUN}")
