print(f"durum : {solver.StatusName(status)}")
print(f"amaç  : {solver.ObjectiveValue():.0f}")
print(f"alt sınır: {solver.BestObjectiveBound():.0f}")
_gap = (solver.ObjectiveValue() - solver.BestObjectiveBound()) / max(1, abs(solver.ObjectiveValue()))
print(f"gap   : %{100*_gap:.1f}")

_tw = _u.groupby(["takim", "hafta"])["tip"].nunique()
_fazla = int((_tw - 1).sum())
print(f"\ntakım-hafta: {len(_tw)}  |  tek tip: {(_tw == 1).sum()}  |  "
      f"tip sayısı dağılımı: {_tw.value_counts().sort_index().to_dict()}")
print(f"H19 gerçekleşen ceza: {W_TAKIM * _fazla}  → amacın %"
      f"{100 * W_TAKIM * _fazla / solver.ObjectiveValue():.0f}'i")
