from forecast_vs_actual_karsilastirma import _to_date_series, _to_slot_30

print('satır sayısı:', len(df_actual), '| index unique mi:', df_actual.index.is_unique)

d = _to_date_series(df_actual['data_date']).dt.normalize()
s = df_actual['min_time_period_value'].map(_to_slot_30)

print('NaT tarih:', d.isna().sum(), '| NaN slot:', s.isna().sum())
print('tarih min/max:', d.min(), '→', d.max(), '| unique gün:', d.nunique())
print('unique slot:', s.nunique())
print(df_actual['min_time_period_value'].value_counts().head(10))
print(df_actual['data_date'].min(), '→', df_actual['data_date'].max())
