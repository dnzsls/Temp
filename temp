_p = pd.DataFrame([{"tarih": ds[5:], "gun": DAY_TR[dt.date.fromisoformat(ds).weekday()],
                    "tip": saat[(ds, v)][0], "talep": talep[(ds, v)]}
                   for ds in PLAN_GUNLER if dt.date.fromisoformat(ds).weekday() < 5
                   for v in gun_vardiyalari[ds] if (ds, v) in talep])
print(_p.pivot_table(index=["tarih", "gun"], columns="tip", values="talep",
                     aggfunc="sum", fill_value=0).to_string())
