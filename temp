import inspect
from weekend_forecast_final import <o fonksiyonun_adı>   # subqueue min hesaplayan fonksiyon
# ya da doğrudan modülün kaynağında:
import weekend_forecast_final, inspect
print("hours desteği:", "is_slot_in_shift(slot, hours['start']" in inspect.getsource(weekend_forecast_final))
