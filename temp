a = "U097099"
print("kota", kota_map.get(a), "| izin", sorted(izin_map.get(a, set())))
print("off_t2", sorted(off_block_map.get(a, set())), "| off_t", sorted(off_soft_map.get(a, set())))
for wk, gl in sorted(haftalar.items()):
    _wd = [ds for ds in gl
           if dt.date.fromisoformat(ds).weekday() < 5
           and dt.date.fromisoformat(ds) not in TATIL
           and any((a, ds, v) in x for v in gun_vardiyalari[ds])]
    print(f"{wk[0]}-W{wk[1]}: müsait hafta içi {len(_wd)} → {_wd}")
