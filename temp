gcol = 'gold_total_call'          # forecast'teki gold kolonun
print('dtype:', df_forecast[gcol].dtype, '| NULL:', df_forecast[gcol].isna().sum(), '/', len(df_forecast))

from forecast_vs_actual_karsilastirma import _to_slot_30
bos = df_forecast[df_forecast[gcol].isna()]
if len(bos):
    print('boş olan gün sayısı:', bos['truncddate'].nunique())
    print(bos['model_data_date'].map(_to_slot_30).value_counts().sort_index())

# karşılaştırma tablosunda saat bazında gold
print(df_cmp.groupby('interval')[['forecast_gold','actual_gold']].sum().to_string())
