a = set(df_actual.loc[df_actual['line_based_main_group']=='kitle_cagrilar',
                      'resource_group_key'].astype(str).str.strip().unique())
b = set(df_aht.loc[df_aht['line_based_main_group']=='kitle_cagrilar',
                   'sub_queue'].astype(str).str.strip().unique())
print('sadece actual:', sorted(a-b))
print('sadece aht   :', sorted(b-a))
print('eşleşen      :', len(a&b))
