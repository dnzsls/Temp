var = [s for s, i in sc.items()
       if i.get("start") == "00:00" and i.get("company") == "outsource"]
print("00:00 outsource vardiyası shift_coverage'ta:", var)
print("atanan:", {s: mi['assignments'].get(s, 0) for s in var})
