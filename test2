print("mesai_gun dağılımı:", roster_2["mesai_gun"].value_counts().sort_index().to_dict())
print("norm_gun  dağılımı:", roster_2["norm_gun"].value_counts().sort_index().to_dict())
normal = [a for a in roster_2.index if a not in ozel_agents]
sifir_hs = [a for a in normal if roster_2.at[a,"mesai_gun"]==0 and roster_2.at[a,"norm_gun"]==0]
print(f"normal agent: {len(normal)}  |  0 mesai+0 norm (hiç hafta sonu yok): {len(sifir_hs)}")
