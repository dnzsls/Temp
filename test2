# 1) agent listesi mükerrer mi?
print("AGENTS:", len(AGENTS), "| benzersiz:", len(set(AGENTS)))

# 2) talep tablosu (gün,vardiya) bazında mükerrer mi?
print("df_talep:", len(df_talep),
      "| benzersiz (tarih,vardiya):", len(df_talep.drop_duplicates(["tarih", "vardiya"])))

# 3) bir günün vardiya listesinde tekrar var mı?
g0 = PLAN_GUNLER[0]
print("ilk gün vardiyaları:", len(gun_vardiyalari[g0]),
      "| benzersiz:", len(set(gun_vardiyalari[g0])))

# 4) U101 gerçekte hangi (gün,vardiya)'larda 1?  gün başına kaç kez sayılıyor?
a0 = "U101"
atamalar = []
for ds in PLAN_GUNLER:
    for v in gun_vardiyalari[ds]:
        if (a0, ds, v) in x and solver.Value(x[(a0, ds, v)]) == 1:
            atamalar.append((ds, v))
print(a0, "→ sayılan atama:", len(atamalar),
      "| benzersiz gün:", len(set(ds for ds, v in atamalar)))
print("ilk 15:", atamalar[:15])
