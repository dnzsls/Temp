from weekly_weekday_pipeline_forecast import get_time_cost_multiplier

# 1) Model bu çarpanı ne hesaplıyor?
print("outsource 00:00 çarpan:", get_time_cost_multiplier('kitle', 'outsource', '00:00', CONFIG_WEEKDAY))
print("inhouse   00:00 çarpan:", get_time_cost_multiplier('kitle', 'inhouse',   '00:00', CONFIG_WEEKDAY))

# 2) Vardiyanın GERÇEK start string'i neye benziyor? (format eşleşiyor mu)
mi = results["2026-08-05"]["kitle"]["mip_info"]
for s, info in mi["shift_coverage"].items():
    if info.get("company") == "outsource" and str(info.get("start","")).startswith("00"):
        print("outsource gece vardiya:", s, "start=", repr(info["start"]))
        break

# 3) Config'e yazdığın anahtarı da göster
print("config outsource çarpan dict:", CONFIG_WEEKDAY['time_cost_multipliers']['kitle']['outsource'])
