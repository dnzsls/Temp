import pandas as pd
import numpy as np

# 1) NaN -> None
df_clean = df.where(pd.notna(df), None)

# 2) numpy tiplerini (int64, float64) normal Python tiplerine indir
def indir(v):
    if isinstance(v, np.generic):
        return v.item()
    return v

df_clean = df_clean.map(indir)
