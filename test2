T = "izmir_kitle | Erman Arı"          # tam takım adını kopyala
ds = "2026-06-01"
for a in takimlar[T]:
    if dt.date.fromisoformat(ds) in izin_map.get(a, set()):
        st = "izin"
    else:
        st = "off"
        for v in gun_vardiyalari[ds]:
            if (a, ds, v) in x and solver.Value(x[(a, ds, v)]) == 1:
                st = saat[(ds, v)][0]; break
    print(f"{a}: {st}")
