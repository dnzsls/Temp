# %% [HÜCRE 6] — TAKIM KOLONU (groupname + teamleader_name) ─────────────
df_tam["takim"] = (df_tam["groupname"].str.strip() + " | "
                   + df_tam["teamleader_name"].str.strip())
print(f"takım sayısı: {df_tam['takim'].nunique()}")
print(df_tam[["agent_user_code", "groupname", "teamleader_name", "takim"]].head())
