toplam_slot   = sum(talep.values())          # ay boyunca tüm inhouse kişi-slot
gun_sayisi    = len(PLAN_GUNLER)
agent_sayisi  = len(AGENTS)

print(f"toplam inhouse kişi-slot (ay)     : {toplam_slot}")
print(f"agent sayısı                       : {agent_sayisi}")
print(f"planlanan gün                      : {gun_sayisi}")
print(f"günlük ortalama inhouse ihtiyaç    : {toplam_slot/gun_sayisi:.0f} kişi/gün")
print(f"agent başına ORTALAMA çalışma      : {toplam_slot/agent_sayisi:.1f} gün/ay")
print(f"22 gün için gereken kapasite       : {agent_sayisi*22}  (mevcut talep: {toplam_slot})")
