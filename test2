# %% [HÜCRE 10] — COVERAGE KISITI ───────────────────────────────────────
# Her (gün, vardiya) kovasında atanan kişi = talep (sert kısıt).
# İzinli agentın o gün değişkeni yok → toplama girmez.
for (ds, v), kisi in talep.items():
    model.Add(sum(x[(a, ds, v)] for a in AGENTS if (a, ds, v) in x) == kisi)

print(f"coverage kısıtı: {len(talep)} kova (her biri 'atanan == talep')")

cov = {}
for (ds, v), kisi in talep.items():
    cov[(ds, v)] = model.Add(sum(x[(a, ds, v)] for a in AGENTS if (a, ds, v) in x) == kisi)
