import pandas as pd

for sutun in df.columns:
    tipler = set()
    for hucre in df[sutun]:
        if not pd.isna(hucre):
            tipler.add(type(hucre).__name__)
    print(sutun, "->", tipler)
