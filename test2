toplam_kota = 0
for a in AGENTS:
    hi_izin = sum(1 for d in izin_map.get(a, set()) if d in GUN_SET and d.weekday() < 5)
    toplam_kota += max(0, AYLIK_IS_GUNU - hi_izin)
print("Σkota:", toplam_kota, " | talep:", sum(talep.values()),
      " | fark (zorlanan fazla):", toplam_kota - sum(talep.values()))
