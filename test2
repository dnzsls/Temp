import pandas as pd

gun_kolonlari = [f'GUN_{i}' for i in range(1, 31)]
for sutun in gun_kolonlari:
    tipler = set(type(h).__name__ for h in df[sutun] if not pd.isna(h))
    print(sutun, "->", tipler)
