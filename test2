gun_kolonlari = [f'GUN_{i}' for i in range(1, 31)]

for k in gun_kolonlari:
    yeni = []
    for hucre in df[k]:
        if pd.isna(hucre):
            yeni.append(0.0)            # null -> 0.0
        else:
            yeni.append(float(hucre))   # değeri float olarak bırak
    df[k] = yeni
