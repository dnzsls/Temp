for ds in plani_olan:
    d = dt.date.fromisoformat(ds)
    if d.weekday() >= 5: continue
    mi = results[ds][QUEUE]["mip_info"]
    fazla = mi.get("surplus_total_added") or 0
    eksik = mi.get("total_shortfall") or 0
    print(ds, d.strftime("%a"), f"fazla={fazla} eksik={eksik} net={fazla-eksik}")
