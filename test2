# 00:00 kovalarının talep vs atanan'ı
print(df_cov[df_cov["saat"].str.startswith("00:00")]
      [["tarih", "saat", "talep", "atanan", "fark"]].to_string(index=False))

print("durum:", solver.StatusName(status))
print("objective:", solver.ObjectiveValue(), " best_bound:", solver.BestObjectiveBound())
