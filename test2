a = "U______"   # planlamada 9 off görünen agent

print("roster satırı (30 gün):")
print(roster.loc[a])

print("\nhücre değer sayımı (off/izin/saat kaç kez):")
print(roster.loc[a].drop("takim").value_counts(dropna=False))   # dropna=False → NaN'ı da göster

print("\ndf_ozet bu agent:")
print(df_ozet.loc[a])
