# %% [HÜCRE 4] — KUYRUĞA SÜZ (line_based_main_group → QUEUE) ────────────
# 'kitle_cagrilar' → 'kitle'. Sadece bu kuyruğun agentlarını tut.
df_agents["kuyruk"] = (df_agents["line_based_main_group"]
                       .str.strip().str.lower().str.replace("_cagrilar", "", regex=False))
df_kuyruk = df_agents[df_agents["kuyruk"] == QUEUE].copy()

print(f"[H4] '{QUEUE}' agent: {len(df_kuyruk)} / {len(df_agents)} (diğer kuyruklar elendi)")
print(df_kuyruk[["agent_user_code", "agent_name",
                 "line_based_main_group", "kuyruk"]].head())
