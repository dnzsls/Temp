# --- 2.5. df_dev'den organizasyon bilgilerini df_emp'ye ekle ---
df_org = df_dev[['sicil', 'grup_sube_name', 'bolum', 'ekip', 'takim']].drop_duplicates(subset='sicil', keep='last')

df_emp = df_emp.merge(
    df_org, 
    left_on='agent_user_code', 
    right_on='sicil', 
    how='left'
).drop('sicil', axis=1)

print(f"Organizasyon bilgisi eşleşen: {df_emp['grup_sube_name'].notna().sum()} / {len(df_emp)}")
